[project]
name = "modernmuse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.12",
    "django-taggit>=6.1.0",
    "pillow>=12.1.1",
    "python-dotenv>=1.2.2",
    "whitenoise>=6.12.0",
]

[dependency-groups]
dev = [
    "pytest-django>=4.12.0",
]
